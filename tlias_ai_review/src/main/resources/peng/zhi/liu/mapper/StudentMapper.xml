<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="peng.zhi.liu.mapper.StudentMapper">
<!--    添加学员-->
    <insert id="addStudentMapper">
        insert into student(name, no, gender, phone, id_card, is_college, address, degree, graduation_date, clazz_id, create_time, update_time)
        values (#{name},#{no},#{gender},#{phone},#{idCard},#{isCollege},#{address},#{degree},#{graduationDate},#{clazzId},#{createTime},#{updateTime})
    </insert>
<!--    修改学员信息-->
    <update id="modifyStudentMapper">
        update student 
        <set>
            <if test="name!=null and name !=''">name = #{name},</if>
            <if test="no!=null and no !=''">no = #{no},</if>
            <if test="phone!=null and phone !=''">phone = #{phone},</if>
            <if test="gender!=null">gender = #{gender},</if>
            <if test="degree!=null">degree = #{degree},</if>
            <if test="idCard!=null and idCard!=''">id_card = #{idCard},</if>
            <if test="isCollege!=null">is_college = #{isCollege},</if>
            <if test="address!=null and address!=''">address = #{address},</if>
            <if test="graduationDate!=null">graduation_date = #{graduationDate},</if>
            <if test="violationCount!=null">violation_count = #{violationCount},</if>
            <if test="violationScore!=null">violation_score = #{violationScore},</if>
            <if test="clazzId!=null">clazz_id = #{clazzId},</if>
            <if test="updateTime!=null">update_time = #{updateTime}</if>
        </set>
        where id = #{id}
    </update>
    <!--    批量删除学员-->
    <delete id="deleteStudentMapper">
        delete from student
        where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
    <!--    分页查询-->
    <select id="selectStudentPageMapper" resultType="peng.zhi.liu.pojo.Student">
        select * from student
        <where>
            <if test="name!=null and name!=''">name = #{name} and</if>
            <if test="degree!=null">degree = #{degree} and</if>
            <if test="clazzId!=null">clazz_id = #{clazzId}</if>
        </where>
        order by update_time desc
    </select>
<!--    根据ID查询学员-->
    <select id="selectStudentByIdMapper" resultType="peng.zhi.liu.pojo.Student">
        select * from student where id = #{id}
    </select>
</mapper>