# 配置spring项目信息
spring:
  application:
    name: tlias_ai_review
    # 配置数据库相关信息
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tlias_ai_review
    username: root
    password: liu6713122616.

  # 配置网络请求相关信息
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB

# 配置mybatis相关信息
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

    # 开启驼峰命名映射开关
    map-underscore-to-camel-case: true

# 配置事务管理的日志级别
logging:
  level:
    org.springframework.jdbc.support.JdbcTransactionManager: debug

    # 阿里云OSS相关配置
aliyun:
  oss:
    endPoint: https://oss-cn-beijing.aliyuncs.com
    bucketName: java-ai-sunset
    region: cn-beijing
